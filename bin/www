#!/usr/bin/env node

var app = require('../app');
var config = require('../config/config');

app.listen(config.server.port,function(){
	console.log('app started, listen on port:', config.server.port);
})